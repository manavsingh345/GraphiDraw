{
  "installCommand": "corepack enable && corepack prepare pnpm@9.15.0 --activate && corepack pnpm install --frozen-lockfile --filter=frontend...",
  "buildCommand": "corepack pnpm --filter frontend run build"
}
